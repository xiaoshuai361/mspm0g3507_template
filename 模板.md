# MSPM0G3507 自制板工程模板说明

先用 CCS 自带 gmake 编译 Debug；推荐用 J-Link + CCS/UniFlash DSLite 从 VS Code 一键烧录 Debug/cy_template.out，DAPLink/OpenOCD 作为备用。

> 工程名：`cy_template`  
> 芯片：TI MSPM0G3507，LQFP-64（PM）  
> 软件架构：`BSP → Module → App`  
> 配置真源：工程根目录 `empty.syscfg`  
> 文档更新时间：2026-07-19

## 1. 工程用途

本工程是为当前 MSPM0G3507 自制板整理的通用 CCS 模板，已经包含板上常用外设和功能模块。新项目可以直接复制本工程，在 `App` 层编写业务逻辑，不需要重新移植底层驱动。

模板当前包含：

- ADC 五向按键
- PA27 电池电压检测和低电量提醒
- 128×64 OLED
- UART0 调试串口、UART2 蓝牙串口、UART3 数据上传串口
- 双路电机 PWM 和四路方向控制
- 左右轮编码器及 X/Z 编码器
- ICM42688 IMU 和软件 I²C
- 逐飞 DL1A ToF 激光测距，保留 DL1B 旧库备用
- 74HC165 八路灰度传感器
- 蓝牙遥控与灰度循迹车辆控制任务
- 蜂鸣器、LED、两个普通 GPIO 按键
- 1 ms 周期定时器、SysTick 毫秒时基
- PID、VOFA+ JustFloat 和串口参数命令接口

默认程序启用五向键、电池电压检测、OLED、UART0、LED 和当前 IMU 诊断入口。电机、编码器、蓝牙、DL1A ToF、灰度传感器及 TIMA1 周期任务可通过 `App_Run()` 中对应调用行开关，防止模板上电后产生非预期动作。

## 2. 软件环境

| 项目      | 当前工程配置                             |
| --------- | ---------------------------------------- |
| MCU       | MSPM0G3507                               |
| 封装      | LQFP-64（PM）                            |
| CPU       | Arm Cortex-M0+                           |
| CPU 主频  | 32 MHz                                   |
| IDE       | Code Composer Studio（CCS）              |
| MSPM0 SDK | 工程构建使用 2.07.00.05                  |
| SysConfig | 1.25.0                                   |
| 编译器    | TI Arm Clang 4.0.4 LTS                   |
| 调试接口  | SWD，J-Link 推荐；DAPLink/CMSIS-DAP 备用 |
| DSLite    | `D:\APPs\TI\Unflsh\dslite.bat`           |
| OpenOCD   | `F:\APPS\openocd\xpack-openocd-0.12.0-7` |

说明：`empty.syscfg` 的元数据中记录了 `mspm0_sdk@2.07.00.06`，而当前 `.cproject` 和 Debug 构建规则实际引用本机的 2.07.00.05。若换电脑或升级 SDK，应在 CCS 中重新选择已安装的产品版本并完整重建，不要手改生成文件。

## 3. 三层软件架构

```text
cy_template/
├─ empty.c                 程序入口，只负责系统初始化和调用 App
├─ empty.syscfg            芯片、引脚、时钟和外设配置源
├─ App/                    任务/业务逻辑层
│  ├─ app.c
│  └─ app.h
├─ Module/                 功能模块层
│  ├─ Key5D/               ADC 五向键解码、消抖和事件
│  ├─ Menu/                五向键两级菜单和 8×16 OLED 页面
│  ├─ OLED/                OLED 显示和字库
│  ├─ Motor/               双路电机方向和 PWM
│  ├─ Encoder/             轮速编码器和 X/Z 编码器
│  ├─ Bluetooth/           UART2 蓝牙收发
│  ├─ Grayscale/           74HC165 八路灰度采集
│  ├─ LineTrace/           灰度原始值到循迹状态解码
│  ├─ IMU/                 ICM42688 和姿态解算
│  ├─ ToF/                 逐飞 DL1A/DL1B 激光测距
│  ├─ Keys/                PA28/PA29 普通按键状态机
│  ├─ Beep/                蜂鸣器
│  └─ PID/                 增量式 PI/PID
├─ BSP/                    板级硬件抽象层
│  ├─ ADC/                 ADC 原始采样
│  ├─ Board/               通用板级辅助函数
│  ├─ Delay/               SysTick 时基和延时
│  ├─ DL1A/                DL1A XS 引脚和 8 位寄存器 I²C
│  ├─ DL1B/                DL1B XS 引脚和 16 位寄存器 I²C，旧库备用
│  ├─ SoftI2C/             PA1/PA0 软件 I²C
│  ├─ Timer/               TIMA1 启动封装
│  └─ UART/                UART0、UART3、VOFA+ 接口
├─ tests/                  编入固件的 Key5D、Menu、DL1A/DL1B 和循迹自检
├─ tools/                  上位机辅助工具
└─ docs/                   其他说明文档
```

各层职责如下：

- `BSP`：只处理 MCU 外设、DriverLib 和 SysConfig 生成宏，不包含具体业务策略。
- `Module`：组合 BSP 接口，形成按键、OLED、电机、IMU 等可复用功能。
- `App`：决定初始化哪些模块、任务周期、状态切换和整机行为。
- `empty.c`：保持最小化，不在入口文件堆放业务代码。

调用方向应保持为：

```text
empty.c → App → Module → BSP → DriverLib/SysConfig
```

不要让 BSP 反向依赖 Module 或 App。

## 4. 完整引脚配置

最终引脚配置以 `empty.syscfg` 为准。`Debug/ti_msp_dl_config.h` 是构建时生成的宏定义，只用于核对，不允许手动编辑。

### 4.1 板载与通用功能

| 功能        | MCU 引脚 | 外设/模式 | 默认程序 | 说明                          |
| ----------- | -------- | --------- | -------: | ----------------------------- |
| SWDIO       | PA19     | SWD       |     使用 | DAPLink 调试数据              |
| SWCLK       | PA20     | SWD       |     使用 | DAPLink 调试时钟              |
| LED         | PB22     | GPIO 输出 |     启用 | 五向键产生按下事件时翻转      |
| 蜂鸣器      | PB21     | GPIO 输出 |   未调用 | `Beep_set()`、`Beep_toggle()` |
| 电磁铁 MOS  | PA26     | GPIO 输出 |     启用 | 高电平打开，低电平关闭        |
| 普通按键 F1 | PA28     | GPIO 输入 |     启用 | 低有效，稳定按下一次切换电磁铁 |
| 普通按键 F2 | PA29     | GPIO 输入 |   未轮询 | 旧工程按键状态机              |

### 4.2 五向按键

| 原理图网络 | MCU 引脚 | ADC 实例 | ADC 通道 | 默认程序 |
| ---------- | -------- | -------- | -------- | -------: |
| `ADC1_1`   | PB24     | ADC0     | CH5      |     启用 |

这里的 `ADC1_1` 是原理图网络名，不代表芯片必须使用 ADC1。MSPM0G3507 LQFP-64 上的 PB24 由 SysConfig 合法映射为：

```text
PB24 → ADC0 input channel 5 → ADC_KEY_ADCMEM_0
```

ADC 使用 VDDA 作为 3.3 V 参考，采样时间为 125 μs，软件触发 sequence 转换。`ADCMEM0` 给五向按键，`ADCMEM1` 给电池电压检测。

### 4.2.1 电池电压检测

| 原理图网络 | MCU 引脚 | ADC 实例 | ADC 通道 | ADCMEM | 默认程序 |
| ---------- | -------- | -------- | -------- | ------ | -------: |
| `BAT_ADC`  | PA27     | ADC0     | CH0      | MEM1   |     启用 |

电池检测分压电阻为 R12=9.09 kΩ、R13=1 kΩ，分压比例约为 10.09。3S 锂电满电 12.6 V 时，PA27 上约为 1.25 V，低于 3.3 V ADC 参考，采样范围安全。

代码在 `BSP/ADC/bsp_adc.c` 中使用整数 mV 换算：

```c
BAT_mV = raw * 33297 / 4095
```

`33297 mV` 是 ADC 满量程 3.3 V 反推到电池端后的电压。App 层默认每 5000 ms 采样一次，低电量阈值在 `App/app_config.h`：

```c
#define APP_BATTERY_LOW_MV (10800U)
```

一级菜单第四行会显示电池电压，例如 `Bat:12.3V`；低于阈值时显示 `LOW BAT CHARGE`，提示需要充电。

### 4.2.2 电磁铁 MOS 与独立按键

| 功能 | MCU 引脚 | 模式 | 电平/触发 | 默认程序 |
| ---- | -------- | ---- | --------- | -------: |
| 电磁铁 MOS 控制 | PA26 | GPIO 输出 | 高电平打开，低电平关闭 | 启用 |
| 独立按键 F1 | PA28 | GPIO 输入，上拉 | 低有效，按下接地 | 启用 |

电磁铁控制入口在 `App/app_electromagnet.c`。上电初始化时会先把 PA26 输出低电平，确保默认关闭。`App_Run()` 中周期调用 `App_ElectromagnetRun()`，每 10 ms 扫描一次 PA28，连续 3 次采样稳定为按下后切换一次开关状态，保持按住不会重复切换。

可直接调用的接口：

```c
App_ElectromagnetSetEnabled(true);   // PA26 输出高电平，打开电磁铁
App_ElectromagnetSetEnabled(false);  // PA26 输出低电平，关闭电磁铁
App_ElectromagnetToggle();           // 翻转当前状态
App_ElectromagnetIsEnabled();        // 读取当前状态
```

UART0 调试输出：

```text
MAG=ON CNT=1
MAG=OFF CNT=2
```

如果你的 PA28 按键硬件是“按下接 3.3 V”的高有效接法，需要把 `App/app_electromagnet.c` 中 `App_ElectromagnetReadButtonPressed()` 的判断反过来。

### 4.3 OLED 与软件 I²C

| 功能         | MCU 引脚 | 模式          | 说明                       |
| ------------ | -------- | ------------- | -------------------------- |
| OLED SCL     | PB9      | GPIO 模拟时序 | OLED 专用软件总线          |
| OLED SDA     | PB8      | GPIO 模拟时序 | OLED 专用软件总线          |
| 通用 I²C SCL | PA1      | 开漏 GPIO     | ICM42688/外部 I²C 模块     |
| 通用 I²C SDA | PA0      | 开漏 GPIO     | ICM42688/外部 I²C 模块     |
| DL1A XS      | PB14     | GPIO 输出     | DL1A 复位/使能脚           |
| DL1A INT     | 未接     | 未配置        | 当前轮询读取，不使用中断脚 |

OLED 和 ICM42688/DL1A 不在同一组引脚上：OLED 驱动直接控制 PB9/PB8；`BSP/SoftI2C` 控制 PA1/PA0。PA1/PA0 应接外部上拉电阻，推荐从 4.7 kΩ 起根据总线长度和速率调整。DL1A 的 7 位 I²C 地址为 `0x29`，可与 ICM42688 共用 PA1/PA0 总线。DL1B 旧库也使用 `0x29`，不要同时挂两个默认地址相同的 ToF 模块。

OLED 驱动中写地址为 `0x78`，对应常见的 7 位 I²C 地址 `0x3C`。

### 4.4 串口

| 用途          | 外设  | TX   | RX   | 波特率 |                          RX 中断 | 默认程序 |
| ------------- | ----- | ---- | ---- | -----: | -------------------------------: | -------: |
| 调试/VOFA+    | UART0 | PA10 | PA11 | 115200 | 已配置，`uart0_init()` 开启 NVIC |     启用 |
| 蓝牙          | UART2 | PA21 | PA22 | 115200 | 已配置，`uart2_init()` 开启 NVIC |   未启动 |
| 灰度/数据上传 | UART3 | PA14 | PA13 | 115200 |                   未配置 RX 中断 |   未调用 |

注意：

- PA10/PA11 是板上的默认调试串口引脚，不建议复用为普通 GPIO。
- PA21 是板级特殊引脚，本模板沿用旧板的 UART2 接线；不要再分配给其他功能。
- UART 通信必须共地，外设 TX 接 MCU RX，外设 RX 接 MCU TX，电平均为 3.3 V TTL。

### 4.5 电机与 PWM

| 功能          | MCU 引脚 | 外设/通道  | 默认程序 |
| ------------- | -------- | ---------- | -------: |
| 左电机 PWM    | PA8      | TIMA0 CCP0 |   未启动 |
| 右电机 PWM    | PA9      | TIMA0 CCP1 |   未启动 |
| 左电机方向 L1 | PB15     | GPIO 输出  |   未调用 |
| 左电机方向 L2 | PB17     | GPIO 输出  |   未调用 |
| 右电机方向 R1 | PB13     | GPIO 输出  |   未调用 |
| 右电机方向 R2 | PB16     | GPIO 输出  |   未调用 |

TIMA0 的 PWM 计数时钟为 1 MHz，周期计数为 2000，边沿对齐时名义 PWM 频率约为 500 Hz。`Set_Speed(PWMA, PWMB)` 接受约 `-2000～2000` 的有符号值：符号控制方向，绝对值控制占空比。首次调用该函数时才会启动 PWM 计数器。

接电机前必须确认驱动芯片的输入极性、供电和共地，并先架空车轮或断开负载测试方向。

### 4.6 编码器

| 功能         | MCU 引脚 | 触发方式    | 中断优先级 |    默认程序 |
| ------------ | -------- | ----------- | ---------: | ----------: |
| 左编码器 A   | PB4      | GPIO 上升沿 |          0 | NVIC 未启用 |
| 左编码器 B   | PB5      | GPIO 上升沿 |          0 | NVIC 未启用 |
| 右编码器 A   | PB23     | GPIO 上升沿 |          0 | NVIC 未启用 |
| 右编码器 B   | PB12     | GPIO 上升沿 |          0 | NVIC 未启用 |
| X/Z 编码器 A | PA30     | GPIO 上升沿 |          1 | NVIC 未启用 |
| X/Z 编码器 B | PA31     | GPIO 上升沿 |          1 | NVIC 未启用 |

GPIOA 和 GPIOB 的编码器中断统一在 `Module/Encoder/Encoder.c` 的 `GROUP1_IRQHandler()` 中处理。使用前调用对应的 `Encoder_Init()` 或 `Encoder_XZ_Enable()`，并检查实际编码器相序和计数方向。

### 4.7 八路灰度传感器

| 74HC165 信号 | MCU 引脚 | 方向 | 说明          |
| ------------ | -------- | ---- | ------------- |
| CLK          | PB25     | 输出 | 串行移位时钟  |
| SL/PL        | PA24     | 输出 | 并行装载/锁存 |
| SDO          | PA25     | 输入 | 串行数据输入  |

调用 `Grayscale_Read()` 更新采样结果，调用 `Grayscale_GetRaw()` 获取 8 位原始数据。UART3 可用于把结果上传到上位机。

### 4.8 定时器与系统节拍

| 资源    | 配置                   | 默认状态 | 用途                                               |
| ------- | ---------------------- | -------- | -------------------------------------------------- |
| SysTick | 32 MHz / 32000         | 自动启动 | 1 ms 系统时基、`BSP_Delay_GetTick()`、`delay_ms()` |
| TIMA1   | 100 kHz，周期 100 计数 | 停止     | 1 ms 周期任务                                      |
| TIMA0   | 1 MHz，周期 2000 计数  | 停止     | 双路电机 PWM                                       |

`TimeA1_Init()` 会开启 TIMA1 NVIC 并启动计数器。当前模板没有替应用层定义 TIMA1 的业务中断函数，因此调用前必须实现 `TIMA1_IRQHandler()`，并在中断内读取和清除 ZERO 事件；否则会进入默认中断处理。中断函数只做计数、置标志等短操作，耗时计算放到 App 主循环。

## 5. 默认程序行为

上电后的调用流程为：

```text
main()
 ├─ SYSCFG_DL_init()       初始化 SysConfig 中的全部硬件配置
 ├─ App_Init()             自检、UART0 和 OLED 初始化
 └─ while (1)
     └─ App_Run()          非阻塞周期任务
```

App 层已拆成小文件，`App/app.c` 只保留初始化和主调度，避免模板越写越乱：

| 文件                 | 职责                                              |
| -------------------- | ------------------------------------------------- |
| `App/app.c`          | `App_Init()`、`App_Run()`、软件自检日志           |
| `App/app_input.c`    | 五向键 ADC 采样、消抖、按键测试页、按键 UART 日志 |
| `App/app_menu.c`     | 菜单状态、菜单显示数据、任务选择                  |
| `App/app_imu.c`      | IMU 首次初始化、10 ms 轮询、有限诊断输出          |
| `App/app_tof.c`      | DL1A 首次初始化、20 ms 轮询、UART0 距离输出       |
| `App/app_vehicle.c`  | 蓝牙命令、灰度循迹、编码器测速和 PID 输出         |
| `App/app_internal.h` | App 层内部共享接口，外部模块不要直接依赖          |

`App_Run()` 应保持短小，只决定“当前打开哪些任务”。具体实现进入对应文件阅读。

### 5.1 OLED 测试页和菜单切换

`App_Key5DTestRun()` 和 `App_MenuRun()` 共用同一个 ADC 扫描器和 OLED，不要同时启用。当前默认显示菜单：

```c
void App_Run(void)
{
    /* OLED 显示任务：按键测试和菜单二选一。 */
    // App_Key5DTestRun();
    App_MenuRun();

    /* 可选模块：取消注释即启用，注释掉即关闭。 */
    App_ImuRun();
    // App_ToFRun();
}
```

需要查看按键诊断页时交换前两行的注释：

```c
App_Key5DTestRun();
// App_MenuRun();
```

测试页每 50 ms 显示 `RAW` 原始 ADC、`NOW` 瞬时判定、`STABLE` 消抖后按键、`CAND` 候选按键及连续次数、`LAST` 最近事件，以及本次上电后的 `MIN/MAX`。测试模式还会通过 UART0 每 100 ms 输出一行 `ADC=... NOW=... STABLE=...`。如果按键时 `RAW` 明显变化但 `NOW` 一直为 `NONE`，应根据实测值校准第 6 节中的 ADC 判定窗口。

### 5.2 两级菜单

OLED 菜单使用 8×16 ASCII 字体，每行高度为 16 像素。128×64 屏幕一次只显示 4 行，因此页面不再显示 `MAIN MENU`、`PARAMETERS`、`CAR STATUS` 这类标题行，也不显示底部 `L:Back`/`R:Enter` 提示，只保留核心内容：

```text
一级菜单
├─ Task Setup
│  ├─ Task 2F
│  ├─ Task 2L
│  ├─ Task 3
│  ├─ Task 4
│  ├─ Task 5
│  └─ Task 6
├─ Speed
└─ Car Status
```

| 五向键 | 菜单行为                                            |
| ------ | --------------------------------------------------- |
| 上     | 选择上一项，首项继续向上会循环到末项                |
| 下     | 选择下一项，末项继续向下会循环到首项                |
| 左     | 从二级页面返回一级菜单，并保留一级选中位置          |
| 右     | 一级菜单进入页面；Task 页面把当前 Task 设为活动任务 |
| 中     | 暂不使用                                            |

任务按键流程以 OLED 显示的 Task 序号为准：

| OLED 项目 | 第一次右键 | 第二次右键 / 完成条件 |
| --------- | ---------- | --------------------- |
| Task 2F / 2L | 发送 `AA 02`，使能香橙派电机 | 松开后再次右键，启动小车循迹并进入计时页 |
| Task 3 | 发送 `AA 03`，等待香橙派 `AA 11` 就绪 | 再次右键发送 `AA 07` 并进入计时页；收到 `AA 12` 后冻结计时 |
| Task 4 | 发送 `AA 04`，使能香橙派电机 | 肉眼确认电机稳定后再次右键，启动小车任务并进入计时页 |
| Task 5 | 发送 `AA 05`，使能香橙派电机 | 肉眼确认电机稳定后再次右键，启动小车任务并进入计时页 |
| Task 6 | 从任务列表单次右键进入位置设置页 | 位置页第一次右键发送 `AA 06 POS` 并使能电机，松开后第二次右键启动小车任务和计时 |

任务结束后按左键返回任务列表；只要香橙派任务仍处于使能状态，退出时就发送 `AA FF` 失能电机。Task 3 收到 `AA 12` 只停止计时，不自动退出，仍由左键执行失能和返回。

`Speed` 和 `Car Status` 是只读二级页面，不进入第三级。Speed 页显示小车目标速度、实际速度、左轮速度和右轮速度；状态页优先显示 IMU 的 yaw/pitch/roll 和 DL1A ToF 距离。模块未提供数据时显示 `--` 或 `OFF`。

业务代码启用对应模块后，可调用 `App_MenuSetCarSpeedData()`、`App_MenuSetImuData()`、`App_MenuSetSpeedData()` 和 `App_MenuSetTofData()` 更新页面。当前模板中 Speed 页的目标速度默认显示 0，实际速度来自 `Motor1_Speed` 和 `Motor2_Speed` 的平均值，左/右轮速度直接显示两个编码器速度；启用 `App_VehicleRun()` 后目标速度由车辆 PID 目标值更新。目标速度、实际速度、姿态角和轮速单位均为 0.1；ToF 单位为 mm；数据失效时调用对应 `App_MenuInvalidate...()` 接口。当前活动任务保存在 `g_active_task`，0 表示未选择；其内部编号由菜单映射，业务判断应以 OLED Task 名称对应的任务入口为准。

### 5.3 一行启用 IMU

IMU 保持 `BSP → Module → App` 分层：`BSP/SoftI2C` 只负责 PA1/PA0 软件 I²C，`Module/IMU` 负责 ICM42688 和姿态解算，`App_ImuRun()` 负责调度与菜单数据绑定。模板可通过 `App_Run()` 中的一行调用控制是否启用；当前为了实板排查焊接问题已经解除注释：

```c
App_MenuRun();
App_ImuRun();
```

不需要再向 `App_Init()` 添加初始化代码。`App_ImuRun()` 第一次被调用时会自动检查并初始化 ICM42688，成功后每 10 ms 更新一次 yaw、pitch、roll，并把 0.1° 单位的数据送到 `Car Status` 页面。初始化失败时只尝试一次，状态页继续显示 `OFF`，UART0 输出失败信息，避免主循环反复初始化。

### 5.4 一行启用 DL1A ToF

当前运行路径已替换为 DL1A，并保持 `BSP → Module → App` 分层：`BSP/DL1A` 负责 PB14 `XS` 和 PA1/PA0 8 位寄存器 I²C 访问，`Module/ToF/dl1a.c` 负责逐飞 DL1A 初始化流程、状态寄存器和距离解析，`App_ToFRun()` 负责 20 ms 轮询、UART0 诊断和菜单数据绑定。DL1B 旧库保留在 `BSP/DL1B` 和 `Module/ToF/dl1b.c/.h`，但当前 App 不再调用。默认关闭，需要时只解除这一行注释：

```c
App_MenuRun();
App_ImuRun();
App_ToFRun();
```

DL1A 模块通常有 6 个引脚：`VCC`、`GND` 加 4 个信号脚。当前工程实际使用其中 3 个信号脚，`INT` 暂不使用：

| DL1A 模块接口           | 接到本板 | 当前代码是否使用 | 说明                                                                    |
| ----------------------- | -------- | ---------------: | ----------------------------------------------------------------------- |
| `VCC`                   | `5V`     |               是 | 逐飞资料标注为 5V 供电                                                  |
| `GND`                   | `GND`    |               是 | 必须与 MSPM0 板共地                                                     |
| `SCL`                   | `PA1`    |               是 | 软件 I²C 时钟，属于 `BSP/SoftI2C`                                       |
| `SDA`                   | `PA0`    |               是 | 软件 I²C 数据，属于 `BSP/SoftI2C`                                       |
| `XS` / `XSHUT` / `SHUT` | `PB14`   |               是 | 复位/使能脚，`DL1A_Init()` 会拉低再拉高重启模块                         |
| `INT` / `GPIO1`         | 不接     |               否 | 当前采用 20 ms 轮询，不走外部中断；后续需要中断模式时再分配 GPIO 中断脚 |

注意：`INT` 不要接到 `PB14`，`PB14` 只给 `XS` 使用。初始化成功输出 `DL1A INIT OK`，随后 UART0 输出 `DL1A DIST=...mm`，菜单 `Car Status` 页显示 `TOF:xxxmm`。初始化失败会输出 `DL1A INIT FAIL STATUS=... BUS=...`。

逐飞例程的 `isr.c` 是外设中断回调分发器，这种“短 ISR 分发”可以借鉴；但不应把 DL1A 的 I²C 读写放进 1 ms 定时中断。I²C 访问和 `delay_ms()` 会拉长中断占用，影响编码器边沿、串口接收和电机控制实时性。本工程用 `SysTick` 提供毫秒 tick，App 主循环按时间片调度 ToF/IMU/菜单；真正的中断只做短操作，例如计数、收字节、置标志。

启用后 UART0 会先探测 `0x68`、`0x69` 两个可能地址，然后以 500 ms 周期发送 10 帧诊断信息：

- `IMU INIT OK ...`：WHO_AM_I 为 `0x47`，初始化成功；随后输出六轴和 YPR 数据。
- `A68:S1/W00 A69:S1/W00`：两个地址都在地址阶段 NACK，应检查 IMU 供电、GND、焊接、PA1/PA0、上拉和 CS 电平。
- `S0/WFF`：软件收到了 ACK 但数据全高，优先检查 SDA 释放、上拉和总线时序。
- `IMU READ FAIL`：初始化后发生 I²C 读取失败。
- `IMU ALL ZERO`：I²C 读取成功但六轴 12 字节全零，姿态解算会停止，避免用无效数据更新菜单。

`S1` 中的 `1` 对应 I²C 地址阶段无应答；`Wxx` 是读到的 WHO_AM_I。ICM42688 使用 I²C 时还必须保证片选 CS 为高电平；AD0 低对应 `0x68`，AD0 高对应 `0x69`。

ICM42688 接线与配置：SCL=PA1、SDA=PA0、7 位 I²C 地址 `0x69`（AD0 为高）、`WHO_AM_I=0x47`、输出速率 100 Hz。PA1/PA0 必须有上拉。可在调试器观察以下变量：

| 变量                  | 含义                     |
| --------------------- | ------------------------ |
| `g_imu_init_complete` | 已执行初始化后为 1       |
| `g_imu_init_ok`       | 初始化成功为 1，失败为 0 |
| `g_imu_update_count`  | 姿态更新次数，应持续增加 |
| `g_imu_yaw_tenths`    | 航向角，单位 0.1°        |
| `g_imu_pitch_tenths`  | 俯仰角，单位 0.1°        |
| `g_imu_roll_tenths`   | 横滚角，单位 0.1°        |

### 5.5 一行启用蓝牙遥控和灰度循迹

车辆控制入口在 `App/app_vehicle.c`，包含 UART2 蓝牙命令、74HC165 灰度循迹、编码器测速和 PID 输出。默认关闭，启用时只取消 `App_Run()` 里这一行注释：

```c
// App_VehicleRun();
```

启用后第一次调用会自动执行 `uart2_init()`、`Encoder_Init()`，并立即 `Set_Speed(0, 0)` 保证初始停车。之后按主循环时间片运行：

| 周期 | 动作 |
| ---- | ---- |
| 20 ms | `MEASURE_MOTORS_SPEED()` 更新左右轮速度 |
| 10 ms | `Grayscale_Read()` 读取 8 路灰度，并解码循迹状态 |
| 20 ms | `PID_Update()` 后调用 `Set_Speed()` 输出 PWM |
| 1000 ms | UART0 输出 `VEH raw=... state=... follow=...` 调试信息 |

蓝牙通过 UART2，波特率 9600，接线见第 4.4 节。命令沿用旧工程，并兼容两种发送方式：

- 手机蓝牙助手常见的 ASCII 字符：发送字符 `'6'` 时，UART 实际收到的是 54，程序会归一化为命令 6。
- 旧遥控器或上位机发送的二进制数值：发送数值 `0x06` 时，也会识别为命令 6。

UART2 接收中断会忽略 `\r`/`\n`，所以蓝牙助手发送 `6\r\n` 不会被回车换行覆盖。车辆任务收到命令后，UART0 会输出类似 `BT RAW=54 CMD=6 FOLLOW=1` 的调试行。

| 命令字节 | 功能 |
| -------- | ---- |
| `1` | 前进，退出循迹 |
| `2` | 后退，退出循迹 |
| `3` | 停止，退出循迹 |
| `4` | 右转，退出循迹 |
| `5` | 左转，退出循迹 |
| `6` | 开启循迹 |
| `7` | 关闭循迹并停车 |

旧工程里 `follow_flag` 虽然被命令 6/7 设置，但 `follow_work()` 没有真正判断它；并且原地转弯用了阻塞 `while`。当前模板已改为非阻塞写法：只有 `g_vehicle_follow_enabled=1` 时灰度加权偏差才会改目标速度，不会卡住菜单、OLED、串口和 IMU/ToF。

蓝牙接收层提供 `Bluetooth_ReadByte()` 原子读取接口，车辆任务使用它一次性读取数据并清标志，避免旧式 `Get_RxFlag()` + `Get_RxData()` 两步读取被 UART2 中断打断。新代码优先使用 `Bluetooth_ReadByte()`；旧接口仅保留兼容。

灰度循迹在 `Module/LineTrace` 中同时提供离散状态和加权连续偏差。离散状态主要用于日志阅读；实际车辆控制使用加权偏差，避免 0/1 状态切换导致过弯跳变。位定义沿用旧工程：

| 灰度 bit | 位置 | 加权值 | 离散状态参考 |
| -------- | ---- | ------ | ------------ |
| bit7 | 最左 | `-35` | `LINE_TRACE_TURN_LEFT` |
| bit6 | 左 2 | `-25` | `LINE_TRACE_GO_LEFT` |
| bit5 | 左 3 | `-15` | `LINE_TRACE_GO_LEFT` |
| bit4 | 中左 | `-5` | `LINE_TRACE_FORWARD` |
| bit3 | 中右 | `+5` | `LINE_TRACE_FORWARD` |
| bit2 | 右 3 | `+15` | `LINE_TRACE_GO_RIGHT` |
| bit1 | 右 2 | `+25` | `LINE_TRACE_GO_RIGHT` |
| bit0 | 最右 | `+35` | `LINE_TRACE_TURN_RIGHT` |

当前车上灰度原始值按低有效处理：白底=1，黑线=0。程序先取反，再对检测到黑线的通道做加权平均：

```text
error = sum(active_bit_weight) / active_count
correction = error * APP_VEHICLE_LINE_DIFF_GAIN
left_target  = base_speed + correction
right_target = base_speed - correction
```

`error > 0` 表示黑线在右侧，此时左轮目标速度增加、右轮目标速度降低，小车向右修正；`error < 0` 则向左修正。`APP_VEHICLE_LINE_DIFF_GAIN` 和 `APP_VEHICLE_LINE_DIFF_MAX` 在 `App/app_config.h` 里集中配置。当前没有检测到黑线时停车。

这对应照片里的前半段控制链：

```text
灰度阵列 → 加权归一化偏差 → 外环生成左右轮差速目标 → 左右轮速度 PID → PWM
```

照片中的“角度环/角速度环”需要稳定 IMU 反馈。当前模板先不强依赖 IMU，因为 IMU 焊接或通信异常时会直接破坏循迹；等 IMU 数据可靠后，再把 yaw/gyro 作为中间环接入。

Speed 页的 `Tgt` 显示左右目标速度平均值，`Act` 显示左右编码器速度平均值；状态页轮速接口也会同步更新。在线调试可观察：

| 变量 | 含义 |
| ---- | ---- |
| `g_vehicle_follow_enabled` | 循迹开关，1 表示启用 |
| `g_vehicle_last_bt_command` | 最近一次蓝牙命令字节 |
| `g_vehicle_line_raw` | 最近一次 8 位灰度原始值 |
| `g_vehicle_line_state` | 最近一次循迹状态枚举值 |
| `g_vehicle_line_error_tenths` | 加权循迹偏差，单位 0.1 路间距，左负右正 |
| `g_vehicle_line_active_count` | 当前检测到黑线的灰度通道数量 |

用于在线调试的全局变量：

| 变量                         | 含义                     |
| ---------------------------- | ------------------------ |
| `g_app_sample_count`         | ADC 采样次数，应持续增加 |
| `g_key5d_self_test_complete` | 自检完成后为 1           |
| `g_key5d_self_test_failures` | 自检失败数，正常为 0     |
| `g_menu_self_test_complete`  | 菜单自检完成后为 1       |
| `g_menu_self_test_failures`  | 菜单自检失败数，正常为 0 |
| `g_active_task`              | 当前活动任务，0 表示未选 |

## 6. 五向键 ADC 判定

当前窗口根据本板 PB24 实测值划分，相邻按键以两档中心值的中点为边界：

| 按键        |   ADC 判定范围 | 参考中心值 |
| ----------- | -------------: | ---------: |
| 上 UP       |     1500～2314 |       1980 |
| 左 LEFT     |     2315～2814 |       2650 |
| 中 CENTER   |     2815～3079 |       2980 |
| 右 RIGHT    |     3080～3239 |       3180 |
| 下 DOWN     |     3240～3649 |       3300 |
| 无按键 NONE | ≥3650 或 <1500 |    约 4000 |

不同电阻误差、VDDA 和按键结构会导致实测值偏移。如果某个方向无法识别：

1. 通过 OLED 或 UART0 记录该方向至少 20 次 ADC 原始值。
2. 取稳定区间的最小值和最大值。
3. 给区间两侧保留适量噪声余量。
4. 修改 `Module/Key5D/key5d.c` 中对应窗口。
5. 重新构建并烧录，逐个验证五个方向和松开状态。

不要通过修改 `Debug/ti_msp_dl_config.c` 来调整按键范围。

## 7. 主要模块和入口 API

| 层级/目录          | 功能                 | 常用接口                                                                              |
| ------------------ | -------------------- | ------------------------------------------------------------------------------------- |
| `BSP/ADC`          | 五向键 ADC 原始采样  | `BSP_ADC_KeyReadRaw()`                                                                |
| `BSP/Delay`        | 1 ms Tick 与阻塞延时 | `BSP_Delay_GetTick()`、`delay_ms()`                                                   |
| `BSP/DL1A`         | DL1A XS/I²C 访问     | `BSP_DL1A_InitPins()`、`BSP_DL1A_ReadRegister()`、`BSP_DL1A_WriteRegister()`          |
| `BSP/DL1B`         | DL1B XS/I²C 访问     | `BSP_DL1B_InitPins()`、`BSP_DL1B_ReadRegister()`、`BSP_DL1B_WriteRegister()`          |
| `BSP/SoftI2C`      | PA1/PA0 软件 I²C     | `IICreadBytes()`、`IICwriteBytes()`                                                   |
| `BSP/Timer`        | 启动 TIMA1           | `TimeA1_Init()`                                                                       |
| `BSP/UART`         | UART0/UART3、VOFA+   | `uart0_init()`、`uart0_send_string()`、`uart3_send_string()`、`vofa_justfloat_send()` |
| `Module/Key5D`     | 五向键解码与事件     | `Key5D_Init()`、`Key5D_Update()`、`Key5D_GetKey()`                                    |
| `Module/Menu`      | 两级菜单和 8×16 渲染 | `Menu_Init()`、`Menu_HandleInput()`、`Menu_Render()`                                  |
| `Module/OLED`      | 128×64 OLED          | `OLED_Init()`、`OLED_Clear()`、`OLED_ShowString()`、`OLED_Refresh()`                  |
| `Module/Motor`     | 双路电机控制         | `Set_Speed()`                                                                         |
| `Module/Encoder`   | 编码器计数和轮速     | `Encoder_Init()`、`MEASURE_MOTORS_SPEED()`                                            |
| `Module/Bluetooth` | UART2 蓝牙           | `uart2_init()`、`uart2_send_string()`、`Bluetooth_ReadByte()`                         |
| `Module/Grayscale` | 八路灰度采集         | `Grayscale_Read()`、`Grayscale_GetRaw()`                                              |
| `Module/LineTrace` | 灰度循迹状态解码     | `LineTrace_DecodeState()`、`LineTrace_StateName()`                                    |
| `Module/IMU`       | ICM42688、姿态数据   | `IMU_init()`、`IMU_getYawPitchRoll()`、`IMU_DegreesToTenths()`                        |
| `Module/ToF`       | DL1A ToF 距离        | `DL1A_Init()`、`DL1A_Update()`、`DL1A_GetDistanceMm()`                                |
| `Module/Keys`      | 普通按键状态机       | `KEY_Init()`、`KEY_Get()`、`KEY_FLAG_Get()`                                           |
| `Module/Beep`      | 蜂鸣器控制           | `Beep_set()`、`Beep_toggle()`                                                         |
| `Module/PID`       | 增量式 PI/PID        | `PID_Update()`                                                                        |
| `App/app_electromagnet.c` | 电磁铁应用任务 | `App_ElectromagnetRun()`、`App_ElectromagnetSetEnabled()`、`App_ElectromagnetToggle()` |
| `App/app_vehicle.c` | 车辆控制任务         | `App_VehicleRun()`、`App_BluetoothRun()`、`App_LineTraceRun()`                        |

VOFA+ JustFloat 帧格式为多个 `float` 后跟帧尾 `00 00 80 7F`。UART0 接收接口还保留了 `KP=1.5`、`KI=0.3`、`KD=0.1`、`S=50` 一类 ASCII 命令的缓存入口，具体参数应用由 App 层完成。

## 8. 首次配置 VS Code 环境

本模板在工程根目录提供一键配置脚本 `setup.ps1`，与 `模板.md` 同级。别人复制或移动整个工程后，先在工程根目录运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\setup.ps1
```

脚本会自动完成：

- 识别当前工程所在路径，重新生成 `.vscode/tasks.json`、`.vscode/settings.json`、`.vscode/launch.json` 和 `c_cpp_properties.json`。
- 优先读取当前工程正在使用的 SDK 版本，例如 `Debug/ccsIncludes.opt` 中的 `mspm0_sdk_2_07_00_05`，不会盲目切到电脑上最新 SDK。
- 检查 VS Code 插件：C/C++、Cortex-Debug、Tasks 状态栏按钮、Memory/Peripheral Viewer 等。
- 检查 CCS `gmake.exe`、TI Arm Clang、MSPM0 SDK、SysConfig、UniFlash/DSLite、OpenOCD、Arm GDB。
- 生成本机路径记录 `.vscode/local.env.json`，该文件只属于当前电脑，已加入 `.gitignore`。
- 自动执行一次 Build 验证，并显示 FLASH/SRAM 占用表。

如果希望缺少 VS Code 插件时自动安装，运行：

```powershell
powershell -ExecutionPolicy Bypass -File .\setup.ps1 -InstallMissing
```

如果缺少 CCS、MSPM0 SDK、SysConfig、UniFlash/DSLite 或 J-Link 驱动这类大软件，脚本会在终端中询问是否安装。能在本机找到安装包时会直接启动安装；找不到时会提示把安装包放到工程根目录的 `installers/` 文件夹，或打开官方下载页。不会在没有确认的情况下静默安装驱动或大型软件。

常用参数：

| 参数 | 作用 |
| ---- | ---- |
| `-InstallMissing` | 自动安装缺失的 VS Code 插件；大软件缺失时默认同意安装提示 |
| `-SkipExtensions` | 跳过 VS Code 插件检查 |
| `-NoBuildVerify` | 只生成配置，不执行 Build 验证 |
| `-NoWrite` | 只检测环境，不写入 `.vscode` 配置 |

运行完成后，执行 `Ctrl+Shift+P -> Developer: Reload Window`，刷新底部状态栏按钮。

## 9. 在 CCS 中构建

### 9.1 CCS 图形界面

1. 用 CCS 导入 `D:\APPs\TI\CCS_workplace\cy_template`。
2. 选择 Debug 配置。
3. 执行 **Project → Clean**。
4. 执行 **Project → Build Project**。
5. 输出文件为 `Debug/cy_template.out`。

### 9.2 命令行构建

在 CCS 已生成 Debug makefile 后，在工程根目录执行：

```powershell
& 'D:\APPs\TI\CCS\ccs\utils\bin\gmake.exe' -C Debug clean all
```

VS Code 中也可以运行任务 `Build: CCS Debug`。

## 10. VS Code 烧录和调试

当前模板推荐使用 **J-Link + CCS/UniFlash DSLite** 烧录。这个路径走 `targetConfigs/MSPM0G3507.ccxml` 中的 `SEGGER J-Link Emulator` 配置，和 CCS 图形界面的 J-Link 调试/下载属于同一套后端，实测比 OpenOCD/J-Link 更稳定，也更接近 CCS 的下载速度。

### 10.1 推荐烧录：J-Link + DSLite

在 VS Code 中运行：

```text
终端 -> 运行任务 -> Flash: J-Link DSLite (Recommended)
```

工程已经给常用任务配置了状态栏按钮。安装推荐扩展 `actboy168.tasks` 后，VS Code 底部状态栏会显示：

```text
Build   Clean   Rebuild   SysConfig   Gen   Flash
```

其中 `Flash` 对应推荐的 `Flash: J-Link DSLite (Recommended)`，点击后会先编译再烧录。若底部没有出现这些按钮，执行 `Ctrl+Shift+P -> Developer: Reload Window`，并确认工作区不是 Restricted Mode。

`Build` 按钮不是直接裸跑 `gmake`，而是调用 `.vscode/scripts/build-ccs-debug.ps1`。这个脚本会先打印工程名、构建类型、输出文件，再调用 CCS 生成的 `Debug/makefile`，最后解析 `Debug/cy_template.map` 中的 `MEMORY CONFIGURATION`，显示 FLASH/SRAM 占用表，例如：

```text
[main] Project: cy_template
[main] Build type: CCS Debug / TI Arm Clang
[proc] Running command: ...gmake.exe -C ...\Debug all -j4
[build] Output file: ...\Debug\cy_template.out

Memory region       Used Size   Region Size   %age Used
FLASH:              47.2 KB        128 KB      36.91%
SRAM:                9.9 KB         32 KB      31.04%
BCR_CONFIG:             0 B         255 B       0.00%
BSL_CONFIG:             0 B         128 B       0.00%

[build] Build Success: ...\Debug\cy_template.out
```

`Flash` 按钮也会在烧录前显示当前程序文件和内存占用表，然后再调用 DSLite 下载。注意：这个内存表来自 TI 链接器生成的 `.map` 文件，不需要迁移到 CMake/GCC。

该任务会先执行 `Build: CCS Debug`，然后调用：

```powershell
cmd.exe /d /c .vscode\scripts\flash-dslite-jlink.cmd `
  "D:\APPs\TI\CCS_workplace\cy_template" `
  "D:\APPs\TI\Unflsh\dslite.bat"
```

脚本内部实际烧录命令等价于：

```powershell
& 'D:\APPs\TI\Unflsh\dslite.bat' `
  --config='D:\APPs\TI\CCS_workplace\cy_template\targetConfigs\MSPM0G3507.ccxml' `
  -e -r 2 -u `
  'D:\APPs\TI\CCS_workplace\cy_template\Debug\cy_template.out'
```

参数含义：

| 参数 | 作用 |
| ---- | ---- |
| `--config=...MSPM0G3507.ccxml` | 使用 CCS 工程里的 J-Link 目标配置 |
| `-e` | 输出详细进度 |
| `-r 2` | 下载后执行 System Reset |
| `-u` | 复位后运行程序 |
| `Debug/cy_template.out` | 当前 CCS Debug 输出文件 |

一次实测成功日志的关键结尾如下：

```text
Loading Program: D:\APPs\TI\CCS_workplace\cy_template\Debug\cy_template.out
Resetting...
System Reset is issued.
Running...
Success
```

如果 VS Code 烧录失败，优先检查：

1. J-Link 是否插好，设备管理器能否看到 `J-Link driver`。
2. CCS、UniFlash、OpenOCD、Cortex-Debug 是否还有残留调试会话占用 J-Link。
3. `Debug/cy_template.out` 是否存在，是否刚刚成功编译。
4. `targetConfigs/MSPM0G3507.ccxml` 是否仍是 `SEGGER J-Link Emulator`。

### 10.2 J-Link / OpenOCD 经验记录

本机实测中，Windows 设备管理器可以看到 J-Link：

```text
USB\VID_1366&PID_0101... J-Link driver
```

但 OpenOCD 使用 `interface/jlink.cfg` 时失败：

```text
Warn : Failed to open device: LIBUSB_ERROR_NOT_SUPPORTED
Error: No J-Link device found
```

这不是固件没有编译、也不是 MSPM0 没接好，而是当前 J-Link 官方 Windows 驱动与 OpenOCD/libusb 后端不匹配。不要为了让 OpenOCD 识别 J-Link 就随手用 Zadig 把 J-Link 驱动改成 WinUSB，否则可能影响 CCS、UniFlash 或 SEGGER 官方工具。当前工程保留 OpenOCD/J-Link 任务作为实验备用，日常烧录优先使用 `Flash: J-Link DSLite (Recommended)`。

### 10.3 DAPLink / OpenOCD 备用烧录

如果改用 DAPLink/CMSIS-DAP，OpenOCD 目标脚本为 `target/ti_mspm0.cfg`。这条路径只建议作为备用烧录方式，不要把 VS Code 绿色调试按钮当作烧录按钮。

推荐流程是：先编译，再用一次性 OpenOCD 命令烧录、校验、复位运行。

```powershell
& 'D:\APPs\TI\CCS\ccs\utils\bin\gmake.exe' -C Debug all

python C:\Users\zhuch\.codex\skills\mspm0-ccs\scripts\openocd_debug.py D:\APPs\TI\CCS_workplace\cy_template `
  --openocd F:\APPS\openocd\xpack-openocd-0.12.0-7\bin\openocd.exe `
  --interface interface/cmsis-dap.cfg `
  --target target/ti_mspm0.cfg `
  --speeds 500 `
  --process-timeout 120 `
  flash --program D:\APPs\TI\CCS_workplace\cy_template\Debug\cy_template.out
```

这条命令会执行：

```text
init
reset init
flash write_image erase Debug/cy_template.out
reset halt
verify_image Debug/cy_template.out
reset run
shutdown
```

VS Code 烧录任务当前按实板试验要求设为最高档 8 MHz，并在写入后显式执行 `reset halt` 再校验。8 MHz 偶尔能够完成，但也出现过 `Could not find MEM-AP` 或少量字节校验差异；最终没有 `diff`、以代码 0 退出才算成功。若失败，把 `.vscode/tasks.json` 中的 `adapter speed 8000` 改回已验证可用的 `adapter speed 500`。较大固件在 500 kHz 下可能静默约 1～2 分钟。若烧录失败，应先排除调试会话占用、USB 链路和 SWD 接线问题，不要直接执行整片擦除。

### 10.4 VS Code 中使用 OpenOCD 任务

如果使用 OpenOCD，在 VS Code 中只使用任务烧录：

```text
终端 -> 运行任务 -> Flash: J-Link OpenOCD
终端 -> 运行任务 -> Flash: J-Link OpenOCD Safe
```

OpenOCD 任务会先执行 `Build: CCS Debug`，再以工程根目录为工作目录调用 OpenOCD 烧录 `{Debug/cy_template.out}`。使用带花括号的相对路径可避免 Windows 反斜杠被 OpenOCD Tcl 当作转义符。

不要点击左上角绿色调试按钮 `Debug: J-Link OpenOCD` 或 `Debug: DAPLink OpenOCD (备用)` 来当作烧录入口。绿色按钮启动的是 Cortex-Debug 调试流程，它会启动常驻 OpenOCD GDB server，让 GDB attach 目标、halt CPU、设置断点并尝试停在 `main`。这和一次性烧录命令不同；如果 MSPM0 当前调试 AP 状态不稳定，GDB attach 可能失败并留下如下错误：

```text
Could not find MEM-AP to control the core
Target not examined yet
Remote communication error. Target disconnected
```

遇到这种情况，先关闭 VS Code 调试会话，确认没有残留 `openocd.exe` / `arm-none-eabi-gdb.exe` 进程，重新插拔 DAPLink 和目标板电源，然后改用上面的“一次性烧录命令”。

### 10.5 VS Code 调试

需要源码级单步调试时，才使用 Cortex-Debug 配置 `Debug: J-Link OpenOCD` 或备用的 `Debug: DAPLink OpenOCD (备用)`。调试动作会 halt CPU，可能影响电机、PWM、定时器和实时控制逻辑。调试失败时不要连续反复点击绿色按钮，应先回到命令行探测或烧录流程确认硬件链路。

### 10.6 调试器配置区别

`targetConfigs/MSPM0G3507.ccxml` 当前是 J-Link 配置，它只服务于 CCS 的对应调试后端。使用 DAPLink 时走 OpenOCD 流程，不要把 `.ccxml` 当作 OpenOCD 配置文件。

如果使用 J-Link 在 CCS 或 DSLite 中烧录/调试，应走 `targetConfigs/MSPM0G3507.ccxml` 对应的 CCS/J-Link 后端；如果使用 DAPLink，则走 OpenOCD 后端。不要同时让 CCS/J-Link、VS Code/Cortex-Debug、DSLite 和 OpenOCD 命令行占用同一个目标板。

### 10.7 常见恢复方法

如果 OpenOCD 只能读到 SWD DPIDR，但读不到 MEM-AP：

```text
SWD DPIDR 0x6ba02477
Could not find MEM-AP to control the core
Debug AP not available
```

说明 DAPLink 能看到 SWD Debug Port，但 MSPM0 核心调试访问不可用。优先按以下顺序处理：

1. 关闭 VS Code 调试会话、CCS 调试会话和所有 OpenOCD/GDB 进程。
2. 重新插拔 DAPLink，目标板断电 5 秒后重新上电。
3. 检查 SWDIO、SWCLK、GND、VTref/3V3 是否接好，必要时接上 nRESET。
4. 用一次性 OpenOCD 烧录命令重试，不要先点绿色调试按钮。
5. 若仍失败，再考虑用 CCS/J-Link 恢复或执行 MSPM0 的 mass erase/factory reset。擦除恢复会清空芯片内容，不要作为默认操作。

### 10.8 VS Code 函数跳转与代码提示

工程已提供 `.vscode/c_cpp_properties.json` 和 `.vscode/settings.json`，配置内容与 CCS Debug 构建保持一致：

- TI Arm Clang 4.0.4 LTS 编译器
- MSPM0 SDK 2.07.00.05
- `App`、`BSP`、`Module`、`tests` 和 `Debug` 头文件路径
- `__MSPM0G3507__`、`__USE_SYSCONFIG__` 编译宏
- C11、Cortex-M0+ 和 UTF-8

需要安装 Microsoft C/C++ 扩展 `ms-vscode.cpptools`。安装后可使用：

- `F12` 或 `Ctrl+单击`：跳转到定义
- `Alt+F12`：内联查看定义
- `Shift+F12`：查找所有引用
- 输入函数名或 `结构体变量.`：显示补全和参数提示

首次打开或修改 SDK 路径后，索引可能需要一些时间。如果仍不能跳转，按 `Ctrl+Shift+P` 依次执行：

1. `C/C++: Select IntelliSense Configuration`，选择 `MSPM0G3507-TI-Clang`。
2. `C/C++: Reset IntelliSense Database`。
3. `Developer: Reload Window`。

`.vscode/c_cpp_properties.json` 中的 SDK 版本必须和 `.cproject`、`Debug/ccsIncludes.opt` 实际构建版本一致，不能只指向电脑上最新的 SDK。

## 11. 修改引脚或外设的正确方法

1. 在 CCS 中打开 `empty.syscfg`。
2. 修改外设实例、引脚、时钟、中断或采样参数。
3. 保存 SysConfig，让 CCS 重新生成配置。
4. 在 `Debug/ti_msp_dl_config.h` 中核对新的宏名。
5. 修改 BSP 中引用的宏名，不要猜测。
6. Clean 后重新 Build。
7. 烧录并在实板验证。

禁止直接修改：

- `Debug/ti_msp_dl_config.c`
- `Debug/ti_msp_dl_config.h`
- `Debug/*.mk`
- `Debug/*.o`、`*.d`、`*.out`、`*.map`
- 其他由 SysConfig 或编译器生成的文件

## 12. 新增功能的推荐方式

以新增一个传感器为例：

1. 在 `empty.syscfg` 中配置 GPIO、ADC、UART、I²C 或 SPI。
2. 在 `BSP/<Peripheral>` 中提供最小硬件读写接口。
3. 在 `Module/<Sensor>` 中实现寄存器、协议、数据转换和状态管理。
4. 在 `App/app.c` 中只放任务开关；具体调度逻辑放到 `App/app_xxx.c`。
5. 将新目录加入 CCS include path 和 source build path。
6. 构建、烧录，并分别验证原始通信、模块数据和整机行为。

建议保持 App 主循环非阻塞，使用 `BSP_Delay_GetTick()` 做周期调度：

```c
static uint32_t lastTick;

void App_Run(void)
{
    const uint32_t now = BSP_Delay_GetTick();

    if ((uint32_t) (now - lastTick) >= 10U) {
        lastTick = now;
        /* 10 ms 周期任务 */
    }
}
```

这种写法能够正确处理 `uint32_t` Tick 回绕，也不会像长时间 `delay_ms()` 那样阻塞其他任务。

## 13. 复制模板后的检查清单

- [ ] 确认目标芯片仍是 MSPM0G3507 LQFP-64。
- [ ] 对照新板原理图检查所有引脚和 3.3 V 电平。
- [ ] 确认 PA19/PA20 未被占用，DAPLink SWD 接线正确并共地。
- [ ] 确认 UART TX/RX 交叉连接，波特率一致。
- [ ] 确认 I²C 有上拉电阻且器件地址正确。
- [ ] 实测并校准五向键的五组 ADC 值。
- [ ] 电机离地测试方向、PWM 极性和限幅。
- [ ] 检查编码器 A/B 相顺序和速度换算参数。
- [ ] 启动 TIMA1 前实现并验证中断函数。
- [ ] 只在 `empty.syscfg` 中改硬件配置，不编辑生成文件。
- [ ] Clean/Build 后确认没有编译错误和警告。
- [ ] 烧录后逐项验证 OLED、串口、按键和需要启用的模块。

## 14. 相关文件

- 工程简要说明：`README.md`
- 一键配置脚本：`setup.ps1`
- 独立引脚表：`docs/pinout.md`
- 硬件配置源：`empty.syscfg`
- 默认业务入口：`App/app.c`
- 五向键实现：`Module/Key5D/key5d.c`
- VS Code 构建、SysConfig、DSLite/OpenOCD 烧录任务：`.vscode/tasks.json`
- VS Code 调试配置：`.vscode/launch.json`
